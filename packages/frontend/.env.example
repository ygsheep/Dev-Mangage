# DevAPI Manager 前端环境变量配置示例
# 复制此文件为 .env.local 并根据实际情况修改配置

# ===== API 服务器配置 =====
# 后端 API 基础 URL（可选，如果不设置将使用下面的主机和端口配置）
# VITE_API_URL=http://localhost:3001

# ===== MCP 服务器配置 =====
# 后端服务器（用于 MCP 管理接口）
VITE_BACKEND_HOST=localhost
VITE_BACKEND_PORT=3001

# MCP HTTP 服务器（用于直接调用 MCP 工具）
VITE_MCP_HTTP_HOST=localhost
VITE_MCP_HTTP_PORT=3001

# MCP WebSocket 服务器（用于实时通信）
VITE_MCP_WS_HOST=localhost
VITE_MCP_WS_PORT=3001

# ===== 开发环境配置示例 =====
# 本地开发环境
# VITE_BACKEND_HOST=localhost
# VITE_BACKEND_PORT=3001
# VITE_MCP_HTTP_HOST=localhost
# VITE_MCP_HTTP_PORT=3320

# ===== 生产环境配置示例 =====
# 生产服务器
# VITE_BACKEND_HOST=your-server.com
# VITE_BACKEND_PORT=3001
# VITE_MCP_HTTP_HOST=your-server.com
# VITE_MCP_HTTP_PORT=3320

# ===== 局域网配置示例 =====
# 局域网访问
# VITE_BACKEND_HOST=192.168.1.100
# VITE_BACKEND_PORT=3001
# VITE_MCP_HTTP_HOST=192.168.1.100
# VITE_MCP_HTTP_PORT=3320

# ===== Docker 配置示例 =====
# Docker 容器环境
# VITE_BACKEND_HOST=backend
# VITE_BACKEND_PORT=3001
# VITE_MCP_HTTP_HOST=mcp-server
# VITE_MCP_HTTP_PORT=3320